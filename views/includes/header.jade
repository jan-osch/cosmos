nav.navbar.navbar-default.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Cosmos
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li(class=isActive('/template/new') ? 'active' : '')
          a(href="/template/new", title="new template") Add
        - if (req.isAuthenticated())
          li
            a(href="/users/"+req.user._id, title="Profile") Profile
          li
            a(href="/auth/logout", title="logout") Logout
        - else
          li(class=isActive('/login') ? 'active' : '')
            a(href="login", title="Login") Login
